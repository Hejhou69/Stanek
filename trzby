<!DOCTYPE html>
<html lang="cs">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Stánek - Tržby</title>
  <style>
    body { font-family: sans-serif; padding: 20px; max-width: 600px; margin: auto; }
    h1 { text-align: center; }
    .sale { margin: 10px 0; }
    .date { font-weight: bold; }
    .amount { font-size: 1.2em; }
    a { display: block; margin-top: 30px; text-align: center; text-decoration: none; font-size: 1em; color: blue; }
  </style>
</head>
<body>
  <h1>Stánek - Tržby</h1>

  <div id="sales"></div>

  <a href="index.html">🏠 Zpět na prodej</a>

  <script>
    function loadSales() {
      const sales = localStorage.getItem('sales');
      return sales ? JSON.parse(sales) : {};
    }

    function renderSales() {
      const salesDiv = document.getElementById('sales');
      salesDiv.innerHTML = ''; // Clear previous content
      const sales = loadSales();

      if (Object.keys(sales).length === 0) {
        salesDiv.innerHTML = '<p>Žádné tržby nejsou uloženy.</p>';
      } else {
        Object.keys(sales).forEach(date => {
          const div = document.createElement('div');
          div.className = 'sale';
          div.innerHTML = `
            <div class="date">${date}</div>
            <div class="amount">${sales[date].reduce((sum, sale) => sum + sale, 0)} Kč</div>
          `;
          salesDiv.appendChild(div);
        });
      }
    }

    renderSales();
  </script>
</body>
</html>
